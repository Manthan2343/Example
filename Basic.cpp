#include<iostream>
using namespace std;
int main(){
    /*int arrary[10];

    cout<<"Value at 0th index:"<< arrary[0]<<endl;
*/
    int limit[10]={1,2,3};
    int n=10;
    for(int i=0;i<n;i++){                   // array should never be greater than or equal to if we start from i=0 as arrary i (n-1) always7

        cout<<limit[i]<<endl;
    }
}